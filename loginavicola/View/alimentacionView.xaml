<UserControl x:Class="loginavicola.View.alimentacionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:loginavicola.View"
             mc:Ignorable="d" Height="819" Width="1207"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="CleanCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="12,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.08" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryGreenButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="＋" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005C13"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#007A1A"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F0F2F5">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="24">

                <StackPanel Margin="12,0,12,20">
                    <TextBlock Text="Control de Alimentación" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                    <TextBlock Text="Administra el consumo diario, semanal e inventario disponible de alimento." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>

                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CleanCardStyle}">
                        <StackPanel>
                            <TextBlock Text="CONSUMO ESTIMADO / AVE" FontSize="12" Foreground="#64748B" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBlock Text="120 kg" FontSize="32" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CleanCardStyle}">
                        <StackPanel>
                            <TextBlock Text="CONSUMO SEMANAL" FontSize="12" Foreground="#64748B" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding ConsumoSemanal, FallbackValue='0 Kg'}" FontSize="32" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CleanCardStyle}">
                        <StackPanel>
                            <TextBlock Text="ALIMENTO DISPONIBLE" FontSize="12" Foreground="#64748B" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding AlimentoDisponible, FallbackValue='7349 Kg'}" FontSize="32" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Margin="12,0,12,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="4" Margin="0,0,16,0" BorderBrush="#E2E8F0" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="16,0,8,0" Foreground="#64748B"/>
                            <TextBox Grid.Column="1" 
                                     Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" Padding="4,8"
                                     x:Name="SearchBox" FontSize="14" Foreground="#1E293B"/>
                        </Grid>
                    </Border>

                    <Button Grid.Column="1" Content="REGISTRAR CONSUMO" Click="BtnRegistrarConsumo_Click" Style="{StaticResource PrimaryGreenButtonStyle}"/>
                </Grid>

                <Border Background="White" CornerRadius="16" Padding="24" Margin="12,0" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Historial de Consumo" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,16"/>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding Consumos}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9" BorderThickness="0" Background="Transparent" RowBackground="White" AlternatingRowBackground="#F8FAFC" RowHeight="44" MinHeight="250">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F1F5F9"/>
                                    <Setter Property="Foreground" Value="#475569"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID consumo/Fecha" Binding="{Binding FechaConsumo, StringFormat=dd/MM/yyyy}" Width="160"/>
                                <DataGridTextColumn Header="Lote" Binding="{Binding IdLoteGallinas}" Width="100"/>
                                <DataGridTextColumn Header="Alimento" Binding="{Binding NombreAlimento}" Width="150"/>
                                <DataGridTextColumn Header="Cantidad" Binding="{Binding CantidadConsumida}" Width="100"/>
                                <DataGridTextColumn Header="Unidad" Binding="{Binding UnidadMedida}" Width="90"/>
                                <DataGridTextColumn Header="Turno" Binding="{Binding Turno}" Width="90"/>
                                <DataGridTextColumn Header="Observaciones" Binding="{Binding Observaciones}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Mostrar:" Foreground="#64748B" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <ComboBox SelectedValue="{Binding ElementosPorPagina, Mode=TwoWay}" Width="65" Height="28">
                                    <System:Int32 xmlns:System="clr-namespace:System;assembly=mscorlib">5</System:Int32>
                                    <System:Int32 xmlns:System="clr-namespace:System;assembly=mscorlib">10</System:Int32>
                                    <System:Int32 xmlns:System="clr-namespace:System;assembly=mscorlib">20</System:Int32>
                                </ComboBox>
                                <TextBlock Text="registros por página" Foreground="#64748B" FontSize="13" VerticalAlignment="Center" Margin="6,0,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="◀" Command="{Binding PaginaAnteriorCommand}" Width="32" Height="28" Margin="0,0,4,0"/>
                                <TextBlock Text="Página " Foreground="#64748B" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding PaginaActual, FallbackValue=1}" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                                <TextBlock Text=" de " Foreground="#64748B" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TotalPaginas, FallbackValue=1}" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <Button Content="▶" Command="{Binding PaginaSiguienteCommand}" Width="32" Height="28"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid x:Name="ModalOverlay" Background="#66000000" Visibility="Collapsed">
            <Border Background="White" CornerRadius="16" Width="420" MaxHeight="580" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="28">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.15" Color="#000000"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Crear Nuevo Registro" FontSize="22" FontWeight="Bold" Foreground="#1E293B"/>
                        <Button Grid.Column="1" Content="✕" Click="BtnCerrarModal_Click" Background="Transparent" BorderThickness="0" Foreground="#64748B" FontSize="18" FontWeight="Bold" Cursor="Hand" VerticalAlignment="Center"/>
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="ID consumo" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                            <TextBox Text="{Binding ConsumoActual.IdConsumo}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>

                            <TextBlock Text="Fecha" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                            <DatePicker SelectedDate="{Binding ConsumoActual.FechaConsumo}" Margin="0,0,0,16" Padding="6"/>

                            <TextBlock Text="Lote" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                            <ComboBox ItemsSource="{Binding LotesActivos}" DisplayMemberPath="IdLote" SelectedValuePath="IdLote" SelectedValue="{Binding ConsumoActual.IdLoteGallinas}" Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,16"/>

                            <TextBlock Text="Alimento" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                            <ComboBox ItemsSource="{Binding Alimentos}" DisplayMemberPath="Nombre" SelectedItem="{Binding AlimentoSeleccionado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Cantidad" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                                    <TextBox Text="{Binding ConsumoActual.CantidadConsumida}" Style="{StaticResource ModernTextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Unidad" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                                    <ComboBox ItemsSource="{Binding UnidadesMedida}" SelectedItem="{Binding ConsumoActual.UnidadMedida}" Style="{StaticResource ModernComboBoxStyle}"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Turno" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                            <ComboBox ItemsSource="{Binding Turnos}" SelectedItem="{Binding ConsumoActual.Turno}" Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,16"/>

                            <TextBlock Text="Observaciones" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6" Foreground="#475569"/>
                            <TextBox Text="{Binding ConsumoActual.Observaciones}" Height="75" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernTextBoxStyle}"/>
                        </StackPanel>
                    </ScrollViewer>

                    <Grid Grid.Row="2" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="＋ Registrar Usuario" Click="BtnGuardar_Click" Style="{StaticResource ActionButtonStyle}" Background="#007A1A" Foreground="White"/>
                        <Button Grid.Column="2" Content="Cancelar" Click="BtnCerrarModal_Click" Style="{StaticResource ActionButtonStyle}" Background="#CBD5E1" Foreground="#475569"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>