<UserControl x:Class="loginavicola.View.reportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:loginavicola.View"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="ReportCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="MinHeight" Value="180"/>
        </Style>

        <Style x:Key="ExportButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,6"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,12,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IconContainerStyle" TargetType="Border">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,20">

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Exportar Datos" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                    <TextBlock Text="Descargue la información del sistema en formato CSV" FontSize="13" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock Text="Exportar Datos" FontSize="24" FontWeight="Bold" Foreground="Black" Margin="0,0,0,5"/>
                    <TextBlock Text="Descargue la información del sistema en formato CSV" FontSize="13" Foreground="Black" Margin="0,0,0,10"/>
                </StackPanel>

                <Grid HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <Border Style="{StaticResource IconContainerStyle}" Background="#4CAF50">
                                <TextBlock Text="📋" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Clasificación de Huevos" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="Registros de categorización" FontSize="10" Foreground="#666" TextWrapping="Wrap"/>
                            <Button Content="EXPORTAR" Style="{StaticResource ExportButtonStyle}" x:Name="BtnExportarClasificacion" Click="BtnExportarClasificacion_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <Border Style="{StaticResource IconContainerStyle}" Background="#00BCD4">
                                <TextBlock Text="📦" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Inventario" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="Stock y suministros" FontSize="10" Foreground="#666" TextWrapping="Wrap"/>
                            <Button Content="EXPORTAR" Style="{StaticResource ExportButtonStyle}" x:Name="BtnExportarInventario" Click="BtnExportarInventario_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <Border Style="{StaticResource IconContainerStyle}" Background="#FFC107">
                                <TextBlock Text="💰" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Insumos" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="Registro de gastos/insumos" FontSize="10" Foreground="#666" TextWrapping="Wrap"/>
                            <Button Content="EXPORTAR" Style="{StaticResource ExportButtonStyle}" x:Name="BtnExportarInsumos" Click="BtnExportarInsumos_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <Border Style="{StaticResource IconContainerStyle}" Background="#9C27B0">
                                <TextBlock Text="🐔" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Lotes de Gallinas" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="Información de aves" FontSize="10" Foreground="#666" TextWrapping="Wrap"/>
                            <Button Content="EXPORTAR" Style="{StaticResource ExportButtonStyle}" x:Name="BtnExportarLotes" Click="BtnExportarLotes_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <Border Style="{StaticResource IconContainerStyle}" Background="#D35400">
                                <TextBlock Text="📈" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Producción Diaria" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="Control de producción" FontSize="10" Foreground="#666" TextWrapping="Wrap"/>
                            <Button Content="EXPORTAR" Style="{StaticResource ExportButtonStyle}" x:Name="BtnExportarProduccion" Click="BtnExportarProduccion_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <Border Style="{StaticResource IconContainerStyle}" Background="#F44336">
                                <TextBlock Text="📊" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Resumen Total" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="Exportación masiva" FontSize="10" Foreground="#666" TextWrapping="Wrap"/>
                            <Button Content="EXPORTAR" Style="{StaticResource ExportButtonStyle}" x:Name="BtnExportarTodo" Click="BtnExportarTodo_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>