<UserControl x:Class="loginavicola.View.reportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900"
             Background="#F7F9FC">

    <UserControl.Resources>
        <!-- Estilo de tarjeta premium -->
        <Style x:Key="ReportCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect 
                BlurRadius="25"    ShadowDepth="8"    Opacity="0.15"     Color="#000000" 
                Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón de acción con estilo limpio -->
        <Style x:Key="ExportButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#2D3436"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E9ECEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="60">
            <!-- Header -->
            <TextBlock Text="Centro de Reportes" FontSize="28" FontWeight="ExtraBold" Foreground="#1A1A1A"/>
            <TextBlock Text="Exporta registros operativos de la unidad avícola." FontSize="14" Foreground="#6C757D" Margin="0,4,0,30"/>

            <!-- Grid de Tarjetas -->
            <UniformGrid Columns="3" VerticalAlignment="Top">

                <!-- Tarjeta 1: Clasificación -->
                <Border Style="{StaticResource ReportCardStyle}">
                    <StackPanel>
                        <TextBlock Text="📋" FontSize="28" Margin="0,0,0,12"/>
                        <TextBlock Text="Clasificación" FontSize="16" FontWeight="Bold" Foreground="#212529"/>
                        <TextBlock Text="Registros de categorización de huevos." FontSize="12" Foreground="#868E96" Margin="0,4,0,20" TextWrapping="Wrap"/>
                        <Button Content="DESCARGAR CSV" Style="{StaticResource ExportButtonStyle}" Click="BtnExportarClasificacion_Click"/>
                    </StackPanel>
                </Border>

                <!-- Tarjeta 2: Inventario -->
                <Border Style="{StaticResource ReportCardStyle}">
                    <StackPanel>
                        <TextBlock Text="📦" FontSize="28" Margin="0,0,0,12"/>
                        <TextBlock Text="Inventario" FontSize="16" FontWeight="Bold" Foreground="#212529"/>
                        <TextBlock Text="Control de stock y suministros." FontSize="12" Foreground="#868E96" Margin="0,4,0,20" TextWrapping="Wrap"/>
                        <Button Content="DESCARGAR CSV" Style="{StaticResource ExportButtonStyle}" Click="BtnExportarInventario_Click"/>
                    </StackPanel>
                </Border>

                <!-- Tarjeta 3: Insumos -->
                <Border Style="{StaticResource ReportCardStyle}">
                    <StackPanel>
                        <TextBlock Text="💰" FontSize="28" Margin="0,0,0,12"/>
                        <TextBlock Text="Insumos" FontSize="16" FontWeight="Bold" Foreground="#212529"/>
                        <TextBlock Text="Registro de gastos e insumos." FontSize="12" Foreground="#868E96" Margin="0,4,0,20" TextWrapping="Wrap"/>
                        <Button Content="DESCARGAR CSV" Style="{StaticResource ExportButtonStyle}" Click="BtnExportarInsumos_Click"/>
                    </StackPanel>
                </Border>

                <!-- Tarjeta 4: Lotes -->
                <Border Style="{StaticResource ReportCardStyle}">
                    <StackPanel>
                        <TextBlock Text="🐔" FontSize="28" Margin="0,0,0,12"/>
                        <TextBlock Text="Lotes" FontSize="16" FontWeight="Bold" Foreground="#212529"/>
                        <TextBlock Text="Información detallada de aves." FontSize="12" Foreground="#868E96" Margin="0,4,0,20" TextWrapping="Wrap"/>
                        <Button Content="DESCARGAR CSV" Style="{StaticResource ExportButtonStyle}" Click="BtnExportarLotes_Click"/>
                    </StackPanel>
                </Border>

                <!-- Tarjeta 5: Producción -->
                <Border Style="{StaticResource ReportCardStyle}">
                    <StackPanel>
                        <TextBlock Text="📈" FontSize="28" Margin="0,0,0,12"/>
                        <TextBlock Text="Producción" FontSize="16" FontWeight="Bold" Foreground="#212529"/>
                        <TextBlock Text="Control de producción diaria." FontSize="12" Foreground="#868E96" Margin="0,4,0,20" TextWrapping="Wrap"/>
                        <Button Content="DESCARGAR CSV" Style="{StaticResource ExportButtonStyle}" Click="BtnExportarProduccion_Click"/>
                    </StackPanel>
                </Border>

                <!-- Tarjeta 6: Resumen -->
                <Border Style="{StaticResource ReportCardStyle}">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="28" Margin="0,0,0,12"/>
                        <TextBlock Text="Resumen" FontSize="16" FontWeight="Bold" Foreground="#212529"/>
                        <TextBlock Text="Exportación masiva del sistema." FontSize="12" Foreground="#868E96" Margin="0,4,0,20" TextWrapping="Wrap"/>
                        <Button Content="DESCARGAR CSV" Style="{StaticResource ExportButtonStyle}" Click="BtnExportarTodo_Click"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>