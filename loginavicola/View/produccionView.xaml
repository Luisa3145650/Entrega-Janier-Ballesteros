<UserControl x:Class="loginavicola.View.produccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F1F5F9">

    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProButton" TargetType="Button">
            <Setter Property="Background" Value="#39A900" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D8600" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ProButton}">
            <Setter Property="Background" Value="#64748B" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#475569" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox x:Name="cbCamaras" Width="200" Height="30" VerticalAlignment="Center" Margin="0,0,15,0"
                              ToolTip="Seleccione la cámara web"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="btnConectarCamara" Content="Conectar" Style="{StaticResource ProButton}" />
                        <Button x:Name="btnDesconectarCamara" Content="Desconectar" Style="{StaticResource SecondaryButton}" />
                        <Button x:Name="btnRefrescarCamaras" Content="Refrescar" Style="{StaticResource SecondaryButton}" />
                        <Button x:Name="btnCapturarFoto" Content="📸 Tomar Foto" Style="{StaticResource SecondaryButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <Grid Background="#1E293B">
                    <TextBlock Text="CÁMARA DESCONECTADA" Foreground="#475569" FontWeight="Bold" FontSize="20"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Image x:Name="imgCamara" Stretch="Uniform" />
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Background="#0F172A" BorderThickness="0">
                <TextBlock x:Name="txtEstadoCamara" Text="Esperando conexión..." 
                           Foreground="#38BDF8" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center"/>
            </Border>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}" BorderBrush="#39A900" BorderThickness="2,0,0,0">
                <StackPanel>
                    <TextBlock Text="LECTURA ACTUAL" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Peso" FontSize="14" Foreground="#94A3B8"/>
                            <TextBlock x:Name="lblPesoReal" Text="0 g" FontSize="42" FontWeight="Black" Foreground="#0F172A"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Categoría" FontSize="14" Foreground="#94A3B8"/>
                            <TextBlock x:Name="lblCategoria" Text="-" FontSize="42" FontWeight="Black" Foreground="#39A900"/>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,15" Background="#E2E8F0"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Volumen estimado (Cámara): " FontSize="16" Foreground="#64748B" FontWeight="SemiBold"/>
                        <TextBlock x:Name="lblVolumen" Text="-" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Button x:Name="btnGuardar" Content="💾 GUARDAR SESIÓN AUTOMÁTICA" Style="{StaticResource ProButton}" FontSize="14" Padding="15,12"/>
                    <Button x:Name="btnClasificacionManual" Content="Ingresar Registro Manual" Style="{StaticResource SecondaryButton}" Click="btnClasificacionManual_Click"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="RESUMEN DE PRODUCCIÓN (HOY)" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,10"/>

                    <UniformGrid Columns="3" Rows="2">
                        <StackPanel Margin="5">
                            <TextBlock Text="JUMBO" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblResumenJumbo" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#8B5CF6"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="AAA" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblResumenAAA" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#3B82F6"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="AA" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblResumenAA" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#10B981"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="A" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblResumenA" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#EAB308"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="B" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblResumenB" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#F97316"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="C" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblResumenC" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#EF4444"/>
                        </StackPanel>
                    </UniformGrid>

                    <Separator Margin="0,10" Background="#E2E8F0"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="TOTAL CLASIFICADOS:" FontSize="14" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,10,0"/>
                        <TextBlock x:Name="lblTotalResumen" Text="0" FontSize="16" FontWeight="Black" Foreground="#39A900"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Style="{StaticResource CardStyle}" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="ÚLTIMOS REGISTROS" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,10"/>
                    <DataGrid x:Name="dgHistorial" Grid.Row="1" AutoGenerateColumns="True" HeadersVisibility="Column" 
                              Background="White" RowBackground="White" AlternatingRowBackground="#F8FAFC"
                              BorderBrush="#E2E8F0" HorizontalGridLinesBrush="#E2E8F0" VerticalGridLinesBrush="Transparent"
                              IsReadOnly="True" />
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>