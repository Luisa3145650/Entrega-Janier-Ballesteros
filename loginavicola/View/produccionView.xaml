<UserControl x:Class="loginavicola.View.produccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1100"
             Background="#F1F5F9">

    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="1" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProButton" TargetType="Button">
            <Setter Property="Background" Value="#39A900" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D8600" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ProButton}">
            <Setter Property="Background" Value="#1E293B" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#334155" />
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="BlueButton" TargetType="Button" BasedOn="{StaticResource ProButton}">
            <Setter Property="Background" Value="#2563EB" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5.5*" />
                <ColumnDefinition Width="4.5*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⚖  BÁSCULA POS (COM3)" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <ComboBox x:Name="cbCamaras" Width="110" Height="28" VerticalAlignment="Center" Margin="0,0,3,0" ToolTip="Seleccione cámara"/>
                            <Button x:Name="btnConectarCamara" Content="🔗" Width="30" Height="28" Margin="2,0" Style="{StaticResource BlueButton}" ToolTip="Conectar Cámara"/>
                            <Button x:Name="btnDesconectarCamara" Content="❌" Width="30" Height="28" Margin="2,0" Style="{StaticResource SecondaryButton}" ToolTip="Desconectar Cámara"/>
                            <Button x:Name="btnRefrescarCamaras" Content="🔄" Width="30" Height="28" Margin="2,0" Style="{StaticResource SecondaryButton}" ToolTip="Refrescar Cámaras"/>
                            <Button x:Name="btnCapturarFoto" Content="📸" Width="35" Height="28" Margin="2,0" Style="{StaticResource ProButton}" ToolTip="Tomar Foto Manual"/>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1" Background="#0F172A" CornerRadius="8" MinHeight="180" Padding="20">
                        <Grid>
                            <Image x:Name="imgCamara" Stretch="Uniform" Opacity="0.25"/>
                            
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="PESO ACTUAL" Foreground="#94A3B8" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="lblPesoReal" Text="0.0 g" Foreground="White" FontSize="64" FontWeight="Black" HorizontalAlignment="Center" Margin="0,5"/>
                                <Border Background="#334155" Padding="10,4" CornerRadius="12" HorizontalAlignment="Center">
                                    <TextBlock Text="Presiona ESPACIO para clasificar" Foreground="#CBD5E1" FontSize="11" FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2" Margin="0,12,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Volumen estimado: " FontSize="13" Foreground="#64748B"/>
                            <TextBlock x:Name="lblVolumen" Text="-" FontSize="13" FontWeight="Bold" Foreground="#0F172A"/>
                        </StackPanel>
                        <TextBlock x:Name="txtEstadoCamara" Text="Sistema Listo" Foreground="#39A900" FontWeight="SemiBold" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="RESUMEN DE PRODUCCIÓN" FontSize="14" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Categoría actual: " FontSize="12" Foreground="#64748B" VerticalAlignment="Center"/>
                            <TextBlock x:Name="lblCategoria" Text="-" FontSize="22" FontWeight="Black" Foreground="#39A900" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <Grid Margin="0,4">
                                <TextBlock Text="🔵 Jumbo" Foreground="#475569" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="lblResumenJumbo" Text="3" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#F1F5F9"/>
                            <Grid Margin="0,4">
                                <TextBlock Text="🟡 AA" Foreground="#475569" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="lblResumenAA" Text="2" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#F1F5F9"/>
                            <Grid Margin="0,4">
                                <TextBlock Text="🟠 B" Foreground="#475569" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="lblResumenB" Text="2" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <Grid Margin="0,4">
                                <TextBlock Text="⚫ AAA" Foreground="#475569" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="lblResumenAAA" Text="1" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#F1F5F9"/>
                            <Grid Margin="0,4">
                                <TextBlock Text="🟢 A" Foreground="#475569" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="lblResumenA" Text="2" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#F1F5F9"/>
                            <Grid Margin="0,4">
                                <TextBlock Text="🔴 C" Foreground="#475569" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="lblResumenC" Text="5" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="2" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="TOTAL HOY: " FontSize="12" FontWeight="Bold" Foreground="#64748B" VerticalAlignment="Center"/>
                            <TextBlock x:Name="lblTotalResumen" Text="15" FontSize="16" FontWeight="Black" Foreground="#39A900" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnClasificacionManual" Content="Clasificación Manual" FontSize="11" Style="{StaticResource SecondaryButton}" Padding="10,6" Click="btnClasificacionManual_Click" Margin="0,0,5,0"/>
                            <Button x:Name="btnGuardar" Content="💾 Guardar" FontSize="11" Style="{StaticResource ProButton}" Padding="12,6"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="📋 HISTORIAL RECIENTE" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,10"/>
                
                <DataGrid x:Name="dgHistorial" Grid.Row="1" AutoGenerateColumns="True" HeadersVisibility="Column" 
                          Background="White" RowBackground="White" AlternatingRowBackground="#F8FAFC"
                          BorderBrush="#E2E8F0" HorizontalGridLinesBrush="#E2E8F0" VerticalGridLinesBrush="Transparent"
                          IsReadOnly="True"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>